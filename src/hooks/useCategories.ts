/**
 * Categories hook — re-exports useCategories from category store.
 */
export { useCategories } from '../store/categoryStore';
export type {
  CategoryApi,
  CreateCategoryRequest,
  UpdateCategoryRequest,
  CategoryTypeApi,
} from '../types/category.types';
