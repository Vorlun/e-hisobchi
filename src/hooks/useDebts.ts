/**
 * Debts hook — re-exports useDebts from debt store.
 */
export { useDebts } from '../store/debtStore';
export type {
  DebtApi,
  DebtSummary,
  DebtFilters,
  CreateDebtRequest,
  UpdateDebtRequest,
} from '../types/debt.types';
