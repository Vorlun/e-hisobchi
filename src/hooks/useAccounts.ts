/**
 * Accounts hook — re-exports useAccounts from account store.
 */
export { useAccounts } from '../store/accountStore';
export type { Account, TotalBalances, CreateAccountRequest, UpdateAccountRequest, DepositWithdrawRequest } from '../types/account.types';
